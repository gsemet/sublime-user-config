<snippet>
    <content><![CDATA[
from functools import wraps

def ${1:decoratorName}(fn):
    @wraps(fn)
    def wrapped():
        ${2:# decorator code}
    return wrapped
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>decorator-function</tabTrigger>
    <description>Function Decorator</description>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.python</scope>
</snippet>
