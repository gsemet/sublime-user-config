<snippet>
    <content><![CDATA[
case ${1:variable_name}; do
    ${2:case1})
        ${3:case 1 commands}
        ;;
    ${4:case2})
        ${5:case 2 commands}
        ;;
    ?)
        ${6:default commands}
        ;;
esac
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>case</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.shell</scope>
</snippet>
